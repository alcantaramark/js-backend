import { MemberProfilePipe } from './member-profile.pipe';

describe('MemberProfilePipe', () => {
  it('create an instance', () => {
    const pipe = new MemberProfilePipe();
    expect(pipe).toBeTruthy();
  });
});
